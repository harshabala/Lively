import Foundation

extension Notification.Name {
    /// Posted when the welcome sheet asks to open a first-video file picker.
    static let livelyRequestFirstVideoPick = Notification.Name("livelyRequestFirstVideoPick")
}
